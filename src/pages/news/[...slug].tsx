import React from 'react';

const CatchAllRoutes = () => {
    return (
        <div>
            <h1>[...slug] is known as catch all routes</h1>
            <p>It is used for url page filtering</p>
        </div>
    );
};

export default CatchAllRoutes;